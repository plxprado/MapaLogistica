package com.mapalogistica.domain;

import static org.junit.Assert.*;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.neo4j.ogm.session.Session;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.annotation.DirtiesContext;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

import com.mapalogistica.PersistenceContext;
import com.mapalogistica.repository.DistanciaRepository;
import com.mapalogistica.repository.PontoRepository;

//@ContextConfiguration(classes = {PersistenceContext.class})
//@RunWith(SpringJUnit4ClassRunner.class)
//@DirtiesContext(classMode = DirtiesContext.ClassMode.AFTER_EACH_TEST_METHOD)
public class DomainTest {

//    @Autowired DistanciaRepository distanciaRepository;
//    @Autowired PontoRepository pontoRepository;  
//    @Autowired Session session;

//    @Test
    public void salvarPonto() {
//        Ponto testPontoA = new Ponto("TEST_A");
//        pontoRepository.save(testPontoA);
//        Ponto fromBDTesteA = pontoRepository.findByNome("TEST_A");
//        assertEquals(testPontoA.getNome(), fromBDTesteA.getNome());
    }
    
   
    
}
